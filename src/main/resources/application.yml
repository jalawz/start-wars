spring:
  datasource:
    url: jdbc:mysql://localhost:3306/starwars?useSSL=false&allowPublicKeyRetrieval=TRUE&serverTimezone=UTC
    username: paulo
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


swapi:
  url: https://swapi.dev/api